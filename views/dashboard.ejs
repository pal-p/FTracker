<!DOCTYPE html>
<html lang="en">

<head>
    <% include ./partials/head %>
    <link rel="stylesheet" type="text/css" href="/css/dashboard.css">
</head>

<body>
    <header>
        <% include ./partials/header %>
    </header>

    <main role="main" class="main container">
        <h1>Dashboard</h1>
        <h2><%=user.username%></h2>
        <ul>
                <%if(user.accounts.length === 0){%>
                    <li>No account yet</li>
                    <%}else{%>

        <%user.accounts.forEach((account)=>{%>
            <li><%=account.name%>:<%=account.total_amount%></li>
        <%})%>
        <%}%>
    </ul>
    <h4>New account</h4>
    <form class="form-inline" action="/account/new" method='POST'>
        <div class="form-group">
            <label for="name">Name</label>
            <input type="text" class="form-control" id="name" name="name" placeholder="Name">
        </div>
        <div class="form-group">
            <label for="amount">Amount</label>
            <input type="number" class="form-control" id="amount" name="total_amount">
        </div>
        <button type="submit" class="btn btn-primary">Submit</button>
    </form>>
    </main>
    <footer>
        <% include ./partials/footer %>
    </footer>
    <% include ./partials/script_foot %>

</body>

</html>